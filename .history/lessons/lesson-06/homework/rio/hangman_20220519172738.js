var words = ["apple", "pear"]
var guesses = 0
const prompt = require('prompt-sync')();
//TODO:rl test
var  guess_letter = prompt('What letter is your guess? ');

while (guesses < 4){
   var word = word[1]
    let wordlength = words[1].length
   for ( i==0;  wordlength; i++)
    if (guess_letter == wordlength ){
        guesses++ 
    }
 
}